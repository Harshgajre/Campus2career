# Client Environment Variables

# Vite Development Server Port
VITE_PORT=5173

# API Base URL (proxied by Vite dev server to backend)
VITE_API_URL=/api

# Note: During development, requests to /api are proxied to http://localhost:5000
# via vite.config.js. In production, update VITE_API_URL to your backend URL.
